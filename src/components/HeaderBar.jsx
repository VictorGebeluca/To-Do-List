import React from 'react';
import { Header } from 'rsuite';

const HeaderBar = () => {
  return (
    <Header className="page-header" style={{ background: '#0f131a', padding: '10px', textAlign: 'center' }}>
  
    </Header>
  );
};

export default HeaderBar;
